// good3.bminor
// Prueba expresiones complejas en la condición de un do-while.

main: function integer () = {
    x: integer = 10;
    y: boolean = true;
    
    // El bucle se detiene cuando x llega a 5
    do {
        print "Decrementando x... ", x;
    } while (--x > 5);
    
    return 0;
}